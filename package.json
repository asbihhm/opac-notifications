{
  "name": "opac-notifications",
  "version": "0.6.0",
  "description": "Send the status of OPAC to a channel in Slack.",
  "author": "asbish",
  "license": "MIT",
  "private": true,
  "bin": {
    "opac-notifications": "bin/opac-notifications"
  },
  "scripts": {
    "start": "./bin/opac-notifications",
    "dev": "NODE_ENV=development ./bin/opac-notifications",
    "test": "mocha \"test/*.spec.js\"",
    "lint": "eslint \"{src,test}/**/*.js\" && prettier --check **/*",
    "lint:fix": "prettier --write '**/*'"
  },
  "dependencies": {
    "@slack/webhook": "^5.0.3",
    "selenium-webdriver": "4.0.0-alpha.7"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^7.2.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-import": "^2.21.2",
    "mocha": "^8.0.1",
    "prettier": "^2.0.5",
    "sinon": "^9.0.2"
  }
}
